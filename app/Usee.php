<?php

namespace App;

use Illuminate\Database\Eloquent\Model;
use Illuminate\Support\Facades\Input as Input;
use Auth;
class Usee extends Model
{
    /*
    public function saveDrugs($date,$price) {
        $this->portion = Input::get("dose");
        $this->id_users = Auth::User()->id;
        $this->id_products = Input::get("name");
        $this->date = $date;
        $this->price = $price;
        //$use->portion = Input::get("portion");
        //$use->portion = Input::get("portion");
        $this->save();
    }
     * 
     */
}
